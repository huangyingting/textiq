# Disabled: TextIQ now uses a main-only workflow with no dev/preview promotion pipeline.
# See .github/skills/git-workflow/SKILL.md and docs/operations/release-gate.md.
name: Squad Promote (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled dev/preview promotion
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "::notice::TextIQ uses main-only PRs and releases. Do not promote dev/preview branches."
