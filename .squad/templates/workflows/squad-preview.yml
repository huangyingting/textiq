# Disabled: TextIQ now uses main-only PRs and releases; no preview branch gate is active.
# See .github/skills/git-workflow/SKILL.md and docs/operations/release-gate.md.
name: Preview Quality Gate (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled preview branch gate
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "::notice::TextIQ uses main-only PRs and releases. The preview branch gate is disabled."
