# Disabled: TextIQ uses a main-only workflow; no insider/insiders release channel is active.
# See .github/skills/git-workflow/SKILL.md and docs/operations/release-gate.md.
name: Squad Insider Release (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled insider release channel
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "::notice::TextIQ uses main-only PRs and releases. The insider release channel is disabled."
