# Disabled: this copied Squad CLI insider release workflow does not match TextIQ's private app release process.
# See docs/operations/release-gate.md for the current TextIQ release gate.
name: Squad Insider Release (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled copied Squad CLI insider release
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "::notice::Disabled copied Squad CLI insider release workflow. Use the TextIQ release gate instead."
