/** Visual-owned type access to presentation theme tokens while legacy fallback remains. */
export type {
  PresentationTheme,
  VisualDefaultsToken,
} from "../document/deck-kernel/presentation-theme";
