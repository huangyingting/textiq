/** Document-owned deck diff for legacy deck previews while v6 fallback remains. */
export { diffDecks } from "../presentation/deck-diff";
export type {
  DeckDiff,
  DeckDiffEntry,
  DeckDiffStatus,
} from "../presentation/deck-diff";
