/** Document-owned deck diff for legacy deck previews while v6 fallback remains. */
export { diffDecks } from "./deck-kernel/deck-diff";
export type {
  DeckDiff,
  DeckDiffEntry,
  DeckDiffStatus,
} from "./deck-kernel/deck-diff";
