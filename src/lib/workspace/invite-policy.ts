/** Largest accepted invite expiry window, guarding against overflow and typos. */
export const MAX_INVITE_EXPIRY_DAYS = 365;

/** Largest accepted invite-link usage cap. */
export const MAX_INVITE_USES_LIMIT = 10_000;
